-- Based on the ARMv7 Series A Manual (Starting at Page 298)

local thumb_opcodes = {
    ADC
}

local arm_opcodes = {
    ADC
}

local thumb_ee_opcodes = {

}

return { thumb_opcodes = thumb_opcodes, arm_opcodes = arm_opcodes, thumb_ee_opcodes = thumb_ee_opcodes }